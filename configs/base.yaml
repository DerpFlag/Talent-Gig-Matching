project_name: talentgig_matching
random_seed: 42
no_fallback: true

weights:
  embedding_similarity: 0.7
  skill_overlap: 0.3

thresholds:
  positive: 0.62
  negative: 0.35

retrieval:
  top_k: 25

labeling:
  positive_top_k: 5
  negative_random_k: 15
